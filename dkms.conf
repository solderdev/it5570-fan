PACKAGE_NAME="it5570-fan"
PACKAGE_VERSION="@PKGVER@"
AUTOINSTALL="yes"

BUILT_MODULE_NAME[0]="it5570_fan"
DEST_MODULE_LOCATION[0]="/kernel/drivers/hwmon/"

# Match the kernel's toolchain: clang-built kernels (e.g. CachyOS) are
# detected from the kernel .config and get LLVM=1; GCC kernels get the
# kbuild defaults. CC/LD/LLVM environment variables still override.
MAKE[0]="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build \
    KDIR=${kernel_source_dir} \
    $(grep -qs '^CONFIG_CC_IS_CLANG=y' ${kernel_source_dir}/.config && echo LLVM=1) \
    ${CROSS_COMPILE:+CROSS_COMPILE=$CROSS_COMPILE} \
    ${CC:+CC=$CC} \
    ${LD:+LD=$LD} \
    ${LLVM:+LLVM=$LLVM} \
    modules"

CLEAN="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build clean"
